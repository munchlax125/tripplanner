# 여행 일정표 HTML 생성 프롬프트

여행 종류를 가리지 않고 쓰도록 만든 범용 템플릿입니다.
**A. 공통 프롬프트**를 복사해 `<< >>`를 채우고, **B. 여행 유형별 추가분**에서 해당하는 것만 골라 뒤에 붙이세요.

---

## A. 공통 프롬프트

```
여행 일정표를 자체 완결형 HTML 파일 하나로 만들어줘.

## 여행 정보
- 기간: << 날짜, 총 N박 M일 >>
- 인원: << 1인 / 2인 / 가족 4인(아이 나이) >>
- 경로: << 출발지 → 경유 → 주요 방문지 → 귀국 >>
- 확정 예약: << 항공편명·시각, 이미 잡은 숙소·티켓 >>
- 예산: << 1인 기준 총액 또는 상한 없음 >>
- 이동 방식: << 대중교통 우선 / 렌터카 / 택시 자유롭게 / 도보 위주 >>
- 이 여행에서 가장 중요한 것: << 예: 사진, 음식, 유적, 휴식, 아이 컨디션 >>
- 절대 놓치면 안 되는 것: << 예약 완료된 공연, 예약 불가한 1회성 이벤트 >>

## 문서 구조
위에서 아래로:
1. 제목 + 부제(기간·인원·경로 한 줄 요약)
2. 숙박 배분 요약 — 도시/지역별 박수를 가로 막대로
3. 이동 수단 블록 (항공편, 기차, 렌터카 픽업·반납)
4. 날짜별 블록 (본문)
5. 확인사항 — 예약·입국서류·복장·결제수단·현지 주의점

## 날짜 블록 구성
- 날짜 + 요일 (요일은 반드시 실제 달력으로 검증)
- 그날의 제목 + 한 줄 부제
- 시간표: `시각 | 활동 | 부연설명(작은 글씨)`
  - 이동 수단을 배지로 구분
  - 요금과 소요시간을 부연설명에 반드시 포함
- 주의 박스: 그날 어긋날 수 있는 지점을 결과까지 적을 것.
  "혼잡할 수 있음"이 아니라 "이 편을 놓치면 다음 차가 4시간 뒤"
- 지역이나 나라가 바뀌는 날은 이동 블록을 따로

## 지도
각 날짜 아래에 그날 경로를 인라인 SVG로:
- 방문 순서대로 번호를 매긴 노드 + 연결선
- 실제 위경도를 등거리원통도법으로 투영 (위도 보정 cos 적용)
- 왼쪽 아래 축척 막대(km), 오른쪽 위 북쪽 표시
- 지명 라벨은 후보 위치를 여러 개 시도해 겹치지 않는 자리에 배치
- 같은 곳을 두 번 가면 번호를 합쳐 표시 ("1·4 서울역")
- 가능하면 실제 도로 선형, 해안선, 호수·하천, 주변 도시를 배경으로
- 맨 위에 전체 경로 개요 지도 한 장
- 공항이 도심에서 멀어 지도가 뭉개지면 공항은 빼고 설명으로 대체

## 디자인
활자 중심의 인쇄물 느낌. 웹사이트가 아니라 편집된 문서처럼.
- 배경 #F2EEE7, 잉크 #241E22, 카드 #F8F5F0, 테두리 rgba(36,30,34,.14)
- 제목은 세리프, 본문은 고딕, 시각·요금·라벨은 고정폭
- 지역(또는 나라)마다 색을 하나씩 배정해 날짜 블록 좌측 레일과 배지에 적용
- 여백을 넉넉히, 테두리는 얇게
- 강조는 색이 아니라 굵기와 여백으로
- 다크모드 대응, 인쇄 시 레이아웃 유지

## 기술 요건
- 파일 하나로 완결. 열었을 때 외부 요청 0건
- 자바스크립트 없이도 전부 보일 것
- 모바일 우선 (세로 화면에서 시간표가 안 깨지게)

## 내용 원칙
- 시간은 실제 운행·개장 정보에 근거할 것. 모르면 모른다고 쓸 것
- 요금은 현지 통화와 원화를 함께
- 요일에 걸리는 항목(휴관일, 주말 장, 감축 운행, 예배 시간)은 반드시 검증
- 낙관적으로 쓰지 말 것. 빠듯하면 빠듯하다고 쓰고 대안을 제시
- 실패 지점을 숨기지 말 것
- 하루에 최소 한 번은 아무것도 배치하지 않은 여백을 둘 것
```

---

## B. 여행 유형별 추가분

**도시 한 곳에 오래 머무는 여행**
```
날짜별이 아니라 구역별로 묶어줘. 같은 동네는 같은 날에 몰고,
날씨가 나쁜 날로 바꿔 넣을 실내 대안을 하루에 하나씩 준비해줘.
색 구분은 나라 대신 구역(동네)으로.
```

**렌터카 로드트립**
```
구간마다 주행거리·예상 시간·주유 지점을 넣어줘.
주차 가능 여부와 요금을 목적지마다 표시하고,
하루 주행 3시간이 넘는 날은 경고를 붙여줘.
지도에는 실제 도로 번호를 표기해줘.
```

**여러 나라를 육로로 넘는 여행**
```
국경 통과를 별도 블록으로 만들고 비자·소요시간·환전을 넣어줘.
나라별로 색을 배정하고, 통화가 바뀌는 지점을 표시해줘.
```

**아이 동반**
```
이동 구간이 2시간을 넘으면 표시하고, 화장실과 식사 가능 지점을 넣어줘.
낮잠 시간대를 이동에 배치하고, 각 장소의 유모차 접근성을 적어줘.
하루 일정은 오후 4시에 끝나게 짜줘.
```

**출장 + 관광**
```
업무 일정을 고정점으로 두고 나머지를 그 사이에 배치해줘.
업무 장소에서 이동 30분 이내인 곳을 우선하고,
복장 때문에 못 가는 곳은 표시해줘.
```

---

## C. 이어서 쓰면 좋은 후속 요청

**예산 연동**
```
이 일정에 맞춰 엑셀 예산서도 만들어줘. 환율 시트를 따로 두고
그 셀만 고치면 전체가 갱신되게. 일자별 합계와 총예산 대비 잔여분까지.
```

**무리한 구간 찾기**
```
이 일정에서 무리한 구간을 찾아줘. 이동 시간, 개장 시각, 요일,
막차 시간을 대조해서 실제로 불가능하거나 빠듯한 곳을 짚어줘.
```

**빠진 곳 검토**
```
이 지역 유명 관광지 중 내 일정에 없는 곳을 알려주고,
넣을 가치가 있는지, 넣으려면 뭘 빼야 하는지 판단해줘.
```

**지도 고도화** — 네트워크가 열린 환경(Claude Code 등)에서
```
SVG 지도에 실제 배경지도를 넣어줘. Natural Earth와 OSM 데이터로
해안선·국경·주요 도로·수계를 벡터로 렌더링해서 기존 SVG 뒤에 깔고,
좌표계와 축척은 지금 것을 그대로 유지해. 외부 요청 없이 오프라인 동작할 것.
```

---

## D. 잘 나오게 하는 요령

**확정된 것부터 주세요.** 항공편·기차·예약 완료된 티켓이 먼저 있어야 나머지가 거기 맞춰 짜입니다. 나중에 바꾸면 교통·숙박·지도·예산이 연쇄적으로 흔들립니다.

**"검증해줘"를 자주 쓰세요.** 요일, 휴관일, 주말 운행 감축은 실제로 자주 틀립니다.

**"이거 비현실적인데"라고 지적하세요.** 지리적으로 말이 안 되는 배치가 생깁니다. 지적하면 고쳐집니다.

**한 번에 하나씩 바꾸세요.** 날짜 하나를 옮기면 전부 연동됩니다. 여러 개를 동시에 바꾸면 어디가 틀어졌는지 추적이 어렵습니다.

**출발 직전에 한 번 더 검증하세요.** 요금과 입국 절차는 몇 달 사이에 바뀝니다. 완성된 일정표를 다시 올리고 "지금 기준으로 달라진 게 있는지 확인해줘"라고 하면 됩니다.
